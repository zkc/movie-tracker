import React from 'react'
import { shallow } from 'enzyme'
import fetchMock from 'fetch-mock'

import App from '../../components/App.js';

describe('App', () => {

  it('should return App components', () => {
    expect(true).toBe(true)
  });
})